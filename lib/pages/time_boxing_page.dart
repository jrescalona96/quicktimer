import 'package:flutter/material.dart';

class TimeBoxingPage extends StatelessWidget {
  const TimeBoxingPage({super.key});
  static const String path = '/time_boxing';
  static const String title = 'Time Boxing';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Sorry! This page is still under construction. Check back in later.',
        style: Theme.of(context).textTheme.headlineMedium,
      ),
    );
  }
}
